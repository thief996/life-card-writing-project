# PROGRESS

## 2026-06-19 加入鬼怪卡圖庫

### 今天完成什麼

- 將使用者放入 `09_抽牌網站/card-pools/鬼怪卡/` 的 6 張鬼怪圖納入版本控管。
- 新增 `09_抽牌網站/card-pools/manifest.json`，集中記錄鬼怪卡圖檔與抽牌文字。
- 更新抽牌程式，讓「抽鬼怪卡」會讀 manifest 並顯示對應鬼怪圖。

### 改了哪些重要檔案

- `09_抽牌網站/card-pools/鬼怪卡/*.png`
  - 新增 6 張鬼怪圖。
- `09_抽牌網站/card-pools/manifest.json`
  - 新增鬼怪卡清單與技能卡示範清單。
- `scripts/draw.js`
  - 改成可從 manifest 載入牌庫。
  - 抽到有圖片的鬼怪卡時，會把結果卡背景換成對應圖片。
- `docs/PROGRESS.md`
  - 補上本次圖庫更新紀錄。

### 做了哪些決策

- 先讓鬼怪卡使用真實圖庫，技能卡暫時保留示範資料。
- manifest 放在 `09_抽牌網站/card-pools/`，未來技能卡與人格晶體也可以用同一份索引擴充。

## 2026-06-19 加入鬼怪與技能水墨圖

### 今天完成什麼

- 使用繪圖模型替「抽鬼怪」與「抽技能」各生成一張水墨打擊感主視覺。
- 將鬼怪圖接到鬼怪卡結果區，將技能圖接到技能卡結果區。
- 因 OneDrive 仍不穩定支援直接寫入大型 PNG，改用 `deck-art.css` 內嵌兩張圖。

### 改了哪些重要檔案

- `index.html`
  - 載入 `deck-art.css`。
- `deck-art.css`
  - 內嵌鬼怪卡與技能卡的生成圖。
- `docs/PROGRESS.md`
  - 記錄本次生成圖與接入方式。

### 做了哪些決策

- 區塊圖不放可讀文字，避免干擾頁面上的「抽鬼怪」「抽技能」標題。
- 鬼怪圖走紅黑水墨衝擊感，技能圖走青金水墨爆發感，呼應目前網站色票。
- 先用 CSS data URI 接圖，未來若 OneDrive 圖片寫入恢復，再改成一般圖片檔引用。

## 2026-06-19 網站配色對齊水墨抬頭圖

### 今天完成什麼

- 將抽牌網站整體配色調整為呼應水墨抬頭圖。
- 使用粉珊瑚、湖水綠、霧藍紫、暖金與紙色作為主要視覺語言。
- 保留既有上下兩段抽牌功能，不改動抽牌邏輯。

### 改了哪些重要檔案

- `styles.css`
  - 更新全站色票。
  - 調整背景層次、按鈕漸層、卡片覆蓋色與標籤色。
- `docs/PROGRESS.md`
  - 補上本次視覺調整紀錄。

### 做了哪些決策

- 以抬頭圖作為網站主視覺基準，不再讓下方抽牌區使用和抬頭圖割裂的配色。
- 保持卡片區塊可讀性優先，背景圖仍以半透明紙色覆蓋，避免文字被圖片干擾。

## 2026-06-19 加入水墨抬頭圖

### 今天完成什麼

- 使用繪圖模型生成「人生打怪圖鑑」彩色水墨風格抬頭圖。
- 將抬頭圖放進網站最上方，作為抽牌頁面的主視覺。
- 因 OneDrive 專案資料夾拒絕直接寫入大型圖片檔，改以 `header-art.css` 內嵌圖片資料，讓網站仍可穩定顯示。

### 改了哪些重要檔案

- `index.html`
  - 新增最上方抬頭圖區塊。
  - 載入 `header-art.css`。
- `styles.css`
  - 新增抬頭圖尺寸、圓角、陰影與手機版高度設定。
- `header-art.css`
  - 內嵌生成圖片資料。
- `docs/PROGRESS.md`
  - 記錄本次視覺素材加入方式與 OneDrive 限制。

### 做了哪些決策

- 保留生成圖原始檔於 Codex 生成圖片資料夾。
- 網站先用 CSS data URI 方式接圖，避免 OneDrive 對大型圖片檔寫入失敗造成缺圖。
- 抬頭圖只作為主視覺，不改動鬼怪卡與技能卡的抽牌互動。

### 目前卡在哪裡

- 專案資料夾目前無法直接寫入這張大型 PNG 圖片檔；若之後 OneDrive 同步狀態恢復，可再改成一般圖片檔引用。

## 2026-06-19 抽牌網站改成鬼怪/技能雙抽

### 今天完成什麼

- 將抽牌網站改為上下兩部分：
  - 上半部：抽鬼怪卡。
  - 下半部：抽技能卡。
- 抽牌按鈕改成兩個，各自控制自己的牌庫與結果區。
- 移除頁面最下面的「牌庫資料夾」展示區。

### 改了哪些重要檔案

- `index.html`
  - 改成兩個抽牌面板，不再顯示底部資料夾卡片。
- `styles.css`
  - 調整上下兩段版面、鬼怪卡與技能卡的視覺區分。
- `scripts/draw.js`
  - 將單一示範牌庫改成 `ghost` 與 `skill` 兩個牌庫，各自記錄抽牌次數與避免連續抽到同一張。
- `docs/PROGRESS.md`
  - 補上本次改版紀錄。

### 做了哪些決策

- 保留 `09_抽牌網站/card-pools/鬼怪卡/`、`09_抽牌網站/card-pools/技能卡/`、`09_抽牌網站/card-pools/人格晶體/` 三個資料夾，方便之後放檔案與做輪抽。
- 頁面先只顯示鬼怪卡與技能卡兩個抽牌區；人格晶體資料夾保留作為後續擴充。
- 目前仍使用 `scripts/draw.js` 裡的示範資料，等正式檔案放進資料夾後再接 manifest。

### 目前卡在哪裡

- 目前還沒有從資料夾自動讀取卡圖；需要下一步建立 manifest 或索引檔。
- 正式鬼怪卡與技能卡檔案尚未放入對應資料夾。

### 下次接手要先看什麼

1. `index.html`
2. `scripts/draw.js`
3. `09_抽牌網站/card-pools/鬼怪卡/`
4. `09_抽牌網站/card-pools/技能卡/`

## 2026-06-19 抽牌網站初版

### 今天完成什麼

- 新增抽牌網站初版，入口為 `index.html`。
- 畫面分成上下兩部分：
  - 上半部是抽牌區，按下按鈕會抽出一張示範卡。
  - 下半部是牌庫區，列出鬼怪卡、技能卡、人格晶體三個資料夾。
- 建立未來輪抽機制要讀取的三個牌庫資料夾。

### 改了哪些重要檔案

- `index.html`
  - 新增抽牌網站頁面結構。
- `styles.css`
  - 新增抽牌網站視覺、上下分區與手機版排版。
- `scripts/draw.js`
  - 新增按鈕抽牌互動，先用示範資料運作。
- `09_抽牌網站/card-pools/鬼怪卡/README.md`
- `09_抽牌網站/card-pools/技能卡/README.md`
- `09_抽牌網站/card-pools/人格晶體/README.md`
  - 建立三個牌庫資料夾與放檔案規則。

### 做了哪些決策

- 抽牌網站先做成純靜態 HTML/CSS/JavaScript，不增加框架或 build step。
- 三個牌庫資料夾放在 `09_抽牌網站/card-pools/`，避免和現有正式圖檔混在一起。
- 目前抽牌按鈕先使用 `scripts/draw.js` 裡的示範資料；等三個牌庫資料夾放入正式檔案後，再製作輪抽清單或 manifest。

### 目前卡在哪裡

- 瀏覽器不能直接掃描本機資料夾內容，所以「放檔案後自動讀取」需要下一步建立 manifest 或檔案索引。
- 三個牌庫資料夾目前只有說明檔，還沒有正式卡圖或資料。

### 下次接手要先看什麼

1. `index.html`
2. `scripts/draw.js`
3. `09_抽牌網站/card-pools/`
4. `styles.css`

### 下次建議動作

- 把正式的鬼怪卡、技能卡、人格晶體檔案放進對應資料夾。
- 建立 `09_抽牌網站/card-pools/manifest.json`，讓抽牌程式可以讀取實際檔案。
- 再加入輪抽規則，例如抽過不重複、三個牌庫分開抽、每天重置一次。

## 2026-06-19 GitHub 交接整理

### 今天完成什麼

- 確認「人生打怪圖鑑」對應的 GitHub repo 是 `thief996/life-card-writing-project`。
- 將 repo clone 到本機資料夾 `C:\Users\User\OneDrive\文件\.Codex\人生打怪圖鑑`。
- 更新 `README.md`，補上已確認的 repository、本機位置、版本控管流程、部署邊界、環境變數、安全注意事項、已知問題與下一步。
- 更新 `docs/PROGRESS.md`，補上本次整理紀錄，讓下一位 AI 或人工接手時可以直接知道目前狀態。

### 改了哪些重要檔案

- `README.md`
  - 新增「專案狀態」，清楚標示 GitHub repo、本機工作資料夾、預設分支與專案型態。
  - 將專案名稱修正為「人生打怪圖鑑」，避免沿用舊稱。
  - 在「啟動方式」補上版本控管常用流程。
  - 在「部署方式」補充 GitHub 只負責版本控管，不等於 IG 或 Notion 已同步發布。
  - 移除「GitHub remote 尚需確認」這類已過時的卡點，改寫成目前真正需要注意的同步與檢查問題。
- `docs/PROGRESS.md`
  - 新增本次 GitHub 交接整理紀錄。
  - 明確記錄本機 clone、文件更新、決策、卡點與下次接手順序。

### 做了哪些決策

- 本次只整理交接文件，不修改牌卡內容、圖片素材、Notion CSV 或既有資料夾分類。
- 以 GitHub repo `life-card-writing-project` 作為版本控管來源，並以本機資料夾 `人生打怪圖鑑` 作為後續工作入口。
- 專案對外與交接名稱使用「人生打怪圖鑑」。
- `README.md` 維持「AI 可接手」導向，優先說清楚用途、工作入口、限制與下次要做什麼。
- `docs/PROGRESS.md` 繼續作為每次交接的第一手日誌，不把進度只留在聊天紀錄。

### 目前卡在哪裡

- Make 自動發布仍是藍圖，尚未在本次確認是否已正式連上 IG 與 Notion。
- 圖片尺寸、同卡多版本、同卡號衝突仍需要後續逐一盤點。
- Notion 與 GitHub 不是自動雙向同步，後續新增或修正牌卡時要手動確認兩邊是否一致。
- 本次沒有新增測試或檢查腳本，圖片尺寸與資料一致性仍依人工巡檢。

### 下次接手要先看什麼

1. `README.md`
2. `docs/PROGRESS.md`
3. `00_專案總覽/專案地圖.md`
4. `02_視覺設計/視覺規格.md`
5. `08_Notion匯入/Notion建置規格.md`
6. `06_素材庫/正式輸出`
7. Notion 牌卡資料庫：<https://app.notion.com/p/35222c576c69445181283c42f7097ea2>

### 下次建議動作

- 先盤點 `06_素材庫/正式輸出` 的圖片尺寸與命名群組。
- 再比對 Notion 牌卡資料庫是否缺圖片、缺頁面或有重複卡號。
- 若要啟用 Make 自動發布，先用測試資料驗證核准、發布、回寫與錯誤紀錄。

## 2026-06-19

### 今天完成什麼

- 將專案整理為更適合 GitHub 版本控管與 AI 交接的狀態。
- 重寫 `README.md`，補上專案用途、目前功能、啟動方式、部署方式、環境變數、已知問題與下一步。
- 新增 `docs/PROGRESS.md`，作為後續每日接手與決策紀錄的固定入口。
- 確認專案目前尚未初始化 Git repo，且本機環境暫時找不到 `git` 指令。

### 改了哪些重要檔案

- `README.md`
  - 從品牌介紹型首頁改成 AI 與協作者可直接接手的專案入口。
  - 加入資料夾說明、Notion 連結、正式圖檔規則、Make 發布規劃與安全注意事項。
- `docs/PROGRESS.md`
  - 新增交接日誌格式。
  - 記錄今天的整理工作、決策、卡點與下次接手順序。

### 做了哪些決策

- `README.md` 以「交接優先」為目的，不再只放世界觀介紹。
- `docs/PROGRESS.md` 作為之後每次交接的第一手進度文件。
- 正式圖檔來源仍以 `06_素材庫/正式輸出` 為唯一自動同步來源。
- 圖片群組規則維持：同名檔案若只差尾端 `(1)`、`(2)`、`(3)`，建立同一個 Notion 頁面；沒有無序號原檔時，從 `(1)` 開始依序放入。
- 自動發布仍視為規劃中，除非後續確認 Make 情境已正式連線並測試成功。

### 目前卡在哪裡

- 目前資料夾沒有 `.git`，還不是 Git repo。
- 系統路徑中找不到 `git` 指令，因此本回合需要另外確認 Git 是否安裝、安裝位置，或改用其他 Git 工具。
- 尚未找到 GitHub remote URL；沒有 remote 時無法 push。
- Notion 與正式輸出圖片之間仍可能有缺漏，需後續巡檢補齊。

### 下次接手要先看什麼

1. `README.md`
2. `docs/PROGRESS.md`
3. `00_專案總覽/專案地圖.md`
4. `02_視覺設計/視覺規格.md`
5. `08_Notion匯入/Notion建置規格.md`
6. `06_素材庫/正式輸出`
7. Notion 牌卡資料庫：<https://app.notion.com/p/35222c576c69445181283c42f7097ea2>

### 下次建議動作

- 先確認 Git 安裝與 GitHub remote。
- 若沒有 remote，建立 GitHub repo 後設定 `origin`。
- 再執行首次 commit 與 push。
- 接著盤點 `06_素材庫/正式輸出` 和 Notion 資料庫，補齊缺漏牌卡頁與圖片。

